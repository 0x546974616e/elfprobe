[package]
name = "elfprobe-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "elfprobe_core"
path = "sources/library.rs"

[dependencies]
elfprobe-macro = { path = "../elfprobe-macro/" }
libc = "0.2"

[features]
unaligned = []
default = [ "unaligned" ]
